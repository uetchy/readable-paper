version: "2"
services:
  web:
    build: .
    ports:
      - 8080:80
    volumes:
      - .:/usr/src/app
    links:
      - redis
    depends_on:
      - redis
  worker:
    build: .
    command: rq worker --url redis://redis:6379
    volumes:
      - .:/usr/src/app
    links:
      - redis
    depends_on:
      - redis
  redis:
    image: redis
